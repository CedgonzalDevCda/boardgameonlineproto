// Declaration des constantes et variables.

const star = document.querySelector('.fa-star');

console.log("star");

// Sélectionner le score d'une question
const selectScore = () => {
    console.log("star");
    // Afficher la note sélectionné
}

star.addEventListener("click", selectScore);