<div class="container">
    <div class="row justify-content-between">
        {{ form_start(form) }}
        <div class="mt-4">
            {{ form_row(form.games) }}
        </div>
        <div class="mt-4">
            {{ form_row(form.nbPlayer) }}
        </div>
        <div class="mt-4">
            {{ form_row(form.selectFriend) }}
        </div>
        <div class="mt-4">
            {{ form_row(form.Inviter) }}
        </div>
        {{ form_end(form) }}
    </div>

</div>