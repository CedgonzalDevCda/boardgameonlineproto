<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
	</head>
	<body>
		<h1>Demande de
			{{ reportBug.fullName }}</h1>

		<p>Email :
			{{ reportBug.email }}</p>
		<p>Sujet :
			{{ reportBug.subject }}</p>
		<p>Message :</p>
		<div>
			{{ reportBug.message|raw }}
		</div>
	</body>
</html>
