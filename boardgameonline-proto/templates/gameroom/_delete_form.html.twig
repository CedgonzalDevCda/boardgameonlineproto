<form method="post" action="{{ path('app_gameroom_delete', {'id': gameroom.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gameroom.id) }}">
    <button class="btn">Delete</button>
</form>
