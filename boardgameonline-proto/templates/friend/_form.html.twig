<div class="container text-center">
    <div class="row justify-content-around">
        <div class="w-50 text-center">
        {{ form_start(form) }}
        {{ form_widget(form) }}

        <button class="mt-2 btn btn-primary w-25">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}
        </div>
    </div>
</div>